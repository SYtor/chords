<h1>Search result: <%= @list.size %> founded</h1>

<% @list.each do |i| %>
  <% if i.author == 0%>
    <p>Artist: <a href="<%= artist_path(i.id) %>"><%= i.title %></a></p>
  <% else %>
    <p>Chord: <a href="<%= chord_path(i.id) %>"><%= i.title %></a></p>
  <% end %>
<% end %>
